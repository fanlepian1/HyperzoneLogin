# HyperZoneLogin Config Comment Translations - English (US)
# This file is loaded automatically to replace translation keys with English comments.
# All keys use the quoted flat format to prevent HOCON from parsing dots as path separators.

# CoreConfig
"config.core.database" = "Database"
"config.core.remap" = "UUID Remapping"
"config.core.misc" = "Miscellaneous"
"config.core.debug" = "Debug"
"config.core.modules" = "Module Switches"
"config.core.vserver" = "Waiting Area Server"
"config.core.messages" = "Messages"

# DatabaseSourceConfig
"config.db.type" = "Database type\nSupported values: SQLITE, MYSQL, MARIADB"
"config.db.sqlite" = "SQLite database configuration"
"config.db.mysql" = "MySQL database configuration"
"config.db.mariadb" = "MariaDB database configuration"
"config.db.table-prefix" = "Database table prefix"
"config.db.pool" = "Connection pool configuration"
"config.db.host" = "Host"
"config.db.port" = "Port"
"config.db.database-name" = "Database name"
"config.db.username" = "Username"
"config.db.password" = "Password"
"config.db.parameters" = "Extra connection parameters"
"config.db.driver-class-name" = "JDBC driver class"
"config.db.sqlite.path" = "Database file path (relative to plugin data directory)"
"config.db.pool.max-pool-size" = "Maximum pool size"
"config.db.pool.min-idle" = "Minimum idle connections"
"config.db.pool.connection-timeout" = "Connection timeout (ms)"
"config.db.pool.idle-timeout" = "Idle connection timeout (ms)"
"config.db.pool.max-lifetime" = "Maximum connection lifetime (ms)"

# RemapConfig
"config.remap.prefix" = "Generation prefix when UUID passthrough is disabled. Use OfflinePlayer for industry-standard offline UUID generation."

# MiscConfig
"config.misc.kill-chat-session" = "Do not send CHAT_SESSION_UPDATE packet to backend servers"

# DebugConfig
"config.debug.log" = "Debug log switches; recommended to configure all entries here"
"config.debug.slow-test" = "Slow-test mode configuration"
"config.debug.log.general" = "General debug log"
"config.debug.log.outpre-trace" = "Floodgate / OutPre pre-login trace log"
"config.debug.log.profile-skin" = "ProfileSkin debug log"
"config.debug.log.backend-compat" = "Backend waiting-area compat trace log"
"config.debug.log.network-rewrite" = "Netty / GameProfile rewrite trace log"
"config.debug.log.yggdrasil-auth" = "Yggdrasil authentication trace log"
"config.debug.slow-test.enabled" = "When enabled, external overVerify calls are ignored; only the waiting-area /over command completes overVerify"

# ModulesConfig
"config.modules.auth-floodgate" = "Floodgate authentication; only active when Floodgate is installed"
"config.modules.auth-offline" = "Offline authentication"
"config.modules.auth-yggd" = "Yggdrasil authentication"
"config.modules.safe" = "Security protection"
"config.modules.profile-skin" = "Skin cache"
"config.modules.data-merge" = "Data migration"

# VServerConfig
"config.vserver.mode" = "Login server mode: backend or outpre. outpre is recommended; use backend if issues arise."
"config.vserver.post-auth-default-server" = "Default server to connect to after authentication"
"config.vserver.remember-requested-server" = "Remember server transfer requests received during authentication"
"config.vserver.backend.fallback-auth-server" = "Velocity server name of the real authentication waiting area"
"config.vserver.backend.player-info-compensation" = "Waiting-area UpsertPlayerInfo/TabList compat filter compensation"
"config.vserver.backend.profile-compensation" = "Profile compensation sync"
"config.vserver.backend.name-hot-change" = "Hot-change name online (lower risk, enabled by default)"
"config.vserver.backend.uuid-hot-change" = "Hot-change UUID online (high risk, disabled by default)"
"config.vserver.outpre.auth-label" = "Logical name of the auth server, used only for logging/status; no registration in Velocity servers needed.\nIf using ViaVersion, add an entry such as outpre-auth = \"127.0.0.1:30066\" in Velocity servers, but do NOT add it to the try list."
"config.vserver.outpre.auth-host" = "Direct connection host of the auth server"
"config.vserver.outpre.auth-port" = "Direct connection port of the auth server"
"config.vserver.outpre.presented-host" = "Host exposed to the backend in the handshake when forwarding; uses authHost if blank"
"config.vserver.outpre.presented-port" = "Port exposed to the backend in the handshake when forwarding; uses authPort if <= 0"
"config.vserver.outpre.presented-player-ip" = "Player source IP exposed to the backend in the handshake; uses real player IP if blank"

# MessagesConfig
"config.messages.default-locale" = "Default locale; used when client locale detection is disabled or no matching locale file exists"
"config.messages.fallback-locale" = "Fallback locale when a message key is missing; recommended to always keep en_us or zh_cn"
"config.messages.use-client-locale" = "Prefer reading the client's locale setting"

# FloodgateAuthConfig
"config.floodgate.strip-prefix" = "Strip the Floodgate player name prefix"
"config.floodgate.pass-uuid" = "Pass through Floodgate UUID"

# ProfileSkinConfig / MineSkinConfig
"config.skin.prefer-upstream-signed" = "Prefer caching textures that already carry a valid signature"
"config.skin.trusted-entries" = "Entry IDs whose signatures are not repaired"
"config.skin.restore-unsigned" = "Repair textures with unofficial signatures"
"config.skin.mineskin" = "MineSkin repair configuration"
"config.skin.mineskin.method" = "Generation method: URL or UPLOAD"
"config.skin.mineskin.retry-upload" = "Retry with the other mode on error"
"config.skin.mineskin.url-endpoint" = "URL mode API endpoint"
"config.skin.mineskin.upload-endpoint" = "Upload mode API endpoint"
"config.skin.mineskin.timeout" = "Request timeout (ms)"
"config.skin.mineskin.user-agent" = "HTTP User-Agent"

# SafeConfig
"config.safe.global-rate-limit" = "Global connection rate limit"
"config.safe.ip-rate-limit" = "Per-IP connection rate limit"
"config.safe.ip-cooldown" = "Temporary cooldown when per-IP threshold is exceeded"
"config.safe.strict-mode" = "Automatic peak-traffic protection mode"
"config.safe.auth-failure" = "Auth-failure linked protection"
"config.safe.username" = "Basic username validation"
"config.safe.rate-limit.max-attempts" = "Maximum attempts allowed within the window"
"config.safe.rate-limit.window-seconds" = "Window duration (seconds)"
"config.safe.username.enable" = "Enable basic username validation"
"config.safe.username.min-length" = "Minimum length"
"config.safe.username.max-length" = "Maximum length"
"config.safe.username.deny-whitespace" = "Deny leading or trailing whitespace in usernames"
"config.safe.username.pattern" = "Allowed username regex; default matches Minecraft traditional username rules"
"config.safe.ip-cooldown.enabled" = "Enable per-IP temporary cooldown"
"config.safe.ip-cooldown.trigger-attempts" = "Number of rate-limit triggers within the window before temporary ban"
"config.safe.ip-cooldown.window-seconds" = "Tracking window duration (seconds)"
"config.safe.ip-cooldown.cooldown-seconds" = "Cooldown duration after trigger (seconds)"
"config.safe.strict-mode.enabled" = "Enable automatic peak-traffic protection mode"
"config.safe.strict-mode.trigger-attempts" = "Number of global connections within the window to enter strict mode"
"config.safe.strict-mode.window-seconds" = "Tracking window (seconds)"
"config.safe.strict-mode.recover-after" = "Duration to maintain strict mode (seconds)"
"config.safe.strict-mode.global-rate-limit" = "Global rate limit in strict mode"
"config.safe.strict-mode.ip-rate-limit" = "Per-IP rate limit in strict mode"
"config.safe.auth-failure.enabled" = "Enable unified auth-failure linked protection"
"config.safe.auth-failure.trigger-attempts" = "Number of auth failures from the same IP within the window before cooldown"
"config.safe.auth-failure.window-seconds" = "Auth-failure tracking window (seconds)"
"config.safe.auth-failure.cooldown-seconds" = "Cooldown duration after trigger (seconds)"

# AuthOfflineConfig
"config.offline.main" = "Offline authentication main settings"
"config.offline.match" = "Player offline-form matching settings"
"config.offline.password" = "Password policy"
"config.offline.login" = "Login protection"
"config.offline.email" = "Email and account recovery"
"config.offline.prompt" = "Prompts"
"config.offline.session" = "Auto-login session"
"config.offline.pass-offline-uuid" = "Pass through offline UUID"
"config.offline.totp" = "TOTP two-factor authentication"
"config.offline.password.min-length" = "Minimum password length"
"config.offline.password.max-length" = "Maximum password length"
"config.offline.password.deny-name" = "Deny password that contains the username"
"config.offline.login.max-attempts" = "Number of wrong attempts before locking login"
"config.offline.login.block-seconds" = "Lock cooldown duration (seconds)"
"config.offline.email.enabled" = "Enable email commands"
"config.offline.email.delivery-mode" = "Recovery code delivery mode: LOG or SMTP"
"config.offline.email.code-length" = "Recovery code length"
"config.offline.email.code-expire" = "Recovery code expiry (minutes)"
"config.offline.email.recovery-cooldown" = "Recovery email request cooldown (seconds)"
"config.offline.email.max-verify-attempts" = "Maximum wrong attempts allowed per recovery code"
"config.offline.email.reset-window" = "Time window to change password after code verification (minutes)"
"config.offline.email.smtp" = "Recovery email template and SMTP configuration"
"config.offline.smtp.server-name" = "Server name displayed in the email"
"config.offline.smtp.host" = "SMTP server host"
"config.offline.smtp.port" = "SMTP port"
"config.offline.smtp.auth" = "Enable SMTP authentication"
"config.offline.smtp.username" = "SMTP username"
"config.offline.smtp.password" = "SMTP password or app-specific password"
"config.offline.smtp.start-tls" = "Enable STARTTLS"
"config.offline.smtp.ssl" = "Use direct SSL connection"
"config.offline.smtp.connection-timeout" = "Connection timeout (ms)"
"config.offline.smtp.read-timeout" = "Read timeout (ms)"
"config.offline.smtp.write-timeout" = "Write timeout (ms)"
"config.offline.smtp.from-address" = "Sender email address"
"config.offline.smtp.from-name" = "Sender display name"
"config.offline.smtp.recovery-subject" = "Recovery email subject; supports placeholders: %server%, %player%"
"config.offline.smtp.recovery-body" = "Recovery email body; supports placeholders: %server%, %player%, %email%, %code%, %minutes%. Use \\n for newlines."
"config.offline.prompt.show-recovery-hint" = "Show email recovery hint on first login"
"config.offline.session.enabled" = "Enable short-term auto-login session"
"config.offline.session.expire-minutes" = "Session expiry (minutes)"
"config.offline.session.bind-ip" = "Bind session to player IP"
"config.offline.session.issue-on-register" = "Issue a session immediately after registration"
"config.offline.totp.enabled" = "Enable TOTP (two-factor authentication)"
"config.offline.totp.issuer" = "Name displayed in the authenticator app"
"config.offline.totp.pending-expire" = "Pending TOTP secret expiry (minutes)"
"config.offline.totp.allow-session-bypass" = "Allow short-term sessions to bypass two-factor authentication"
"config.offline.match.enable" = "Allow matching"
"config.offline.match.uuid" = "UUID matching settings"
"config.offline.match.host" = "Host matching settings"
"config.offline.match.uuid.zero" = "Allow all-zero UUID (Zalith) to match as offline"
"config.offline.match.uuid.offline" = "Allow default UUID generation method to match as offline"
"config.offline.match.uuid.pcl2" = "PCL2 launcher matching details"
"config.offline.match.pcl2.enable" = "Enable PCL2 UUID matching"
"config.offline.match.pcl2.hash" = "Match PCL2 UUID via hash calculation"
"config.offline.match.pcl2.slim" = "Match PCL2 slim-model UUID"

# EntryConfig
"config.entry.id" = "Entry ID (case-insensitive); used for internal identification"
"config.entry.name" = "Alias used for display purposes"
"config.entry.yggdrasil" = "Authentication service configuration"
"config.entry.yggdrasil.url" = "hasJoined verification URL"
"config.entry.yggdrasil.pass-uuid" = "Pass through Yggdrasil UUID"
"config.entry.yggdrasil.timeout" = "Verification request timeout (ms)"
"config.entry.yggdrasil.retry" = "Retry count"
"config.entry.yggdrasil.retry-delay" = "Retry request delay (ms)"
"config.entry.yggdrasil.proxy" = "Proxy settings"
"config.entry.proxy.type" = "Proxy type\nDIRECT - direct connection or no proxy\nHTTP - proxy for high-level protocols (HTTP/FTP)\nSOCKS - SOCKS (V4 or V5) proxy"
"config.entry.proxy.hostname" = "Proxy server hostname"
"config.entry.proxy.port" = "Proxy server port"
"config.entry.proxy.username" = "Proxy authentication username; leave blank to disable auth"
"config.entry.proxy.password" = "Proxy authentication password"

# MergeMlConfig
"config.merge-ml.source" = "Database configuration"
"config.merge-ml.tables" = "Table name configuration"
"config.merge-ml.service-id-mapping" = "MultiLogin service ID to HZL entry ID mapping; defaults to ml_{serviceId} if not configured"
"config.merge-ml.source.type" = "Database type; supported values: H2DB, MYSQL"
"config.merge-ml.source.h2" = "H2 configuration"
"config.merge-ml.source.mysql" = "MySQL configuration"
"config.merge-ml.h2.jdbc-url" = "Optional: specify JDBC URL directly; auto-generated from path + parameters if blank"
"config.merge-ml.h2.path" = "File path (relative to plugin data directory)"
"config.merge-ml.h2.parameters" = "JDBC extra parameters"
"config.merge-ml.h2.username" = "Username"
"config.merge-ml.h2.password" = "Password"
"config.merge-ml.mysql.host" = "Host"
"config.merge-ml.mysql.port" = "Port"
"config.merge-ml.mysql.database" = "Database name"
"config.merge-ml.mysql.username" = "Username"
"config.merge-ml.mysql.password" = "Password"
"config.merge-ml.mysql.parameters" = "JDBC extra parameters"
"config.merge-ml.tables.user-data-table" = "UserDataTableV3 table name"
"config.merge-ml.tables.in-game-profile-table" = "InGameProfileTableV3 table name"

# MergeAmConfig
"config.merge-am.source" = "Database configuration"
"config.merge-am.tables" = "Table configuration"
"config.merge-am.source.type" = "Database type; supported values: SQLITE, MYSQL"
"config.merge-am.source.sqlite" = "SQLite configuration"
"config.merge-am.source.mysql" = "MySQL configuration"
"config.merge-am.sqlite.jdbc-url" = "Optional: specify JDBC URL directly; auto-generated from path + parameters if blank"
"config.merge-am.sqlite.path" = "SQLite file path (relative to plugin data directory)"
"config.merge-am.sqlite.parameters" = "Optional: JDBC extra parameters"
"config.merge-am.mysql.host" = "Host"
"config.merge-am.mysql.port" = "Port"
"config.merge-am.mysql.database" = "Database name"
"config.merge-am.mysql.username" = "Username"
"config.merge-am.mysql.password" = "Password"
"config.merge-am.mysql.parameters" = "Optional: JDBC extra parameters"
"config.merge-am.tables.authme-table" = "Table name"